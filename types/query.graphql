type Query {
  getUser(id: ID!): User
  getAllUsers: [User]
  getUsersCar(color:String!): [User]
  getAllCars: [Car]
  getAllCarsByUser(model:String!, registrationNo:String!) : [Car]
  testString: String
}

